html
    body
        h1  Welcome to add a  course page
        hr
        h2 Fill the following details
            form(action="/add" method="POST")
                input(type="text" name="username" placeholder="Added by- username")
                br
                input(type="text" name="id" placeholder="Course id")
                br
                input(type="text" name="title" placeholder="Course title")
                br
                input(type="text" name="startDate" placeholder="YYYY-MM-DD" required pattern="(?:19|20)[0-9]{2}-(?:(?:0[1-9]|1[0-2])-(?:0[1-9]|1[0-9]|2[0-9])|(?:(?!02)(?:0[1-9]|1[0-2])-(?:30))|(?:(?:0[13578]|1[02])-31))" title="Enter a date in this format YYYY-MM-DD")
                br
                input(type="text" name="status" value="Active" readonly)
                br
                input(type="Submit" value="Submit")
            br
        span #{message}